#!/bin/sh

PROFILE="--profile-directory=Profile-Main"

if [[ "$1" == --profile-directory=* ]]; then
    PROFILE="$1"
    shift
fi

echo $PATH
exec /usr/bin/chromium --enable-features=UseOzonePlatform --ozone-platform=wayland $PROFILE "$@"