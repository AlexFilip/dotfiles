#!/bin/sh

cd "$HOME"

if ! /usr/local/bin/emacsclient -e 0 >&/dev/null; then
    echo "Starting emacs server" > /dev/stderr
    /usr/local/bin/emacs --daemon
fi

exec /usr/local/bin/emacsclient -c "${@}"

